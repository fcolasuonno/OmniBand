<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Location (required for BLE scan on older APIs) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wake lock to keep connection alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Vibration for alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Network state for sync -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />

    <queries>
        <package android:name="com.urbandroid.sleep" />
    </queries>

    <application
        android:name=".OmniBandApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_watch"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_watch"
        android:supportsRtl="true"
        android:theme="@style/Theme.OmniBand">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Device Connection Service - Foreground service to maintain BLE connection -->
        <service
            android:name=".service.DeviceService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Boot receiver to reconnect on device startup -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Sleep as Android integration receiver -->
        <receiver
            android:name=".service.SleepAsAndroidReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.urbandroid.sleep.watch.START_TRACKING" />
                <action android:name="com.urbandroid.sleep.watch.STOP_TRACKING" />
                <action android:name="com.urbandroid.sleep.watch.SET_PAUSE" />
                <action android:name="com.urbandroid.sleep.watch.SET_SUSPENDED" />
                <action android:name="com.urbandroid.sleep.watch.SET_BATCH_SIZE" />
                <action android:name="com.urbandroid.sleep.watch.START_ALARM" />
                <action android:name="com.urbandroid.sleep.watch.STOP_ALARM" />
                <action android:name="com.urbandroid.sleep.watch.UPDATE_ALARM" />
                <action android:name="com.urbandroid.sleep.watch.SHOW_NOTIFICATION" />
                <action android:name="com.urbandroid.sleep.watch.HINT" />
                <action android:name="com.urbandroid.sleep.watch.CHECK_CONNECTED" />

                <action android:name="com.urbandroid.sleep.alarmclock.SLEEP_TRACKING_STARTED" />
                <action android:name="com.urbandroid.sleep.alarmclock.SLEEP_TRACKING_STOPPED" />
                <action android:name="com.urbandroid.sleep.alarmclock.ALARM_ALERT_START" />
                <action android:name="com.urbandroid.sleep.alarmclock.ALARM_ALERT_DISMISS" />
                <action android:name="com.urbandroid.sleep.alarmclock.ALARM_ALERT_SNOOZE_CANCELLED" />
                <action android:name="com.urbandroid.sleep.alarmclock.ALARM_SNOOZE_CLICKED" />
            </intent-filter>
        </receiver>

        <!-- Bluetooth state change receiver -->
        <receiver
            android:name=".service.BluetoothStateReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
